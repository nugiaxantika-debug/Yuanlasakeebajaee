import btch from "btch-downloader";
import ruhend from "ruhend-scraper";
import { ytdl } from "ruhend-scraper";

console.log("btch keys:", Object.keys(btch));
console.log("ruhend keys:", Object.keys(ruhend));
